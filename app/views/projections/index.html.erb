
<table class="table table-striped">
  <thead>
    <tr>
      <th>Player</th>
      <th>Projection</th>
      <th>Game Date</th>
      <th>Home</th>
      <th>Away</th>
    </tr>
  </thead>

  <tbody>
    <% @projections.each do |proj| %>
      <tr>
        <td><%= proj.player.name %></td>
        <td><%= proj.fp.to_s %></td>
        <td><%= proj.scheduled_game.start_date %></td>
        <td><%= proj.scheduled_game.home_team.name %></td>
        <td><%= proj.scheduled_game.away_team.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

