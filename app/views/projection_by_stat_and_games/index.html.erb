
<table class="table table-striped">
  <thead>
    <tr>
      <th>Stat</th>
      <th>Projection</th>
      <th>Projection(weighted)</th>
    </tr>
  </thead>

  <tbody>
    <% @projection_by_stat_and_games.each do |pbsg| %>
      <tr>
        <td><%= pbsg.criteria %></td>
        <td><%= link_to pbsg.fp.to_s, projection_projection_by_stat_projection_by_stat_and_games_path(pbs.projection, pbs) %></td>
        <td><%= pbsg.weighted_fp %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

