var fantasysport = angular.module('fantasysport', ['ngResource','ngSanitize'])

fantasysport.config	(function($interpolateProvider){
    $interpolateProvider.startSymbol('{{');
    $interpolateProvider.endSymbol('}}');
})

